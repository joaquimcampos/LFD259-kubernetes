apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world
spec:
  completions: 5
  parallelism: 5
  # activeDeadlineSeconds: 30
  template:
    metadata: {}
    spec:
      containers:
      - command:
        - sh
        - -c
        - echo hello; sleep 30; echo world
        image: busybox
        name: hello-world
        resources: {}
      restartPolicy: Never
status: {}
