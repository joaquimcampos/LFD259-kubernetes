apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: canary-balancer
  name: canary-balancer
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    type: nginx
  type: LoadBalancer
status:
  loadBalancer: {}
