# create secrete
kubectl create secret generic mysql --from-literal=password=root

# create configmap
kubectl create configmap colors \
  --from-literal=text=black \
  --from-file=./color.config/favorite \
  --from-file=./color.config/primary/

# kubectl create configmap colors --from-env-file=./color.config/.env
