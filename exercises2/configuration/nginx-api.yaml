apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
  namespace: secret-ops
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
    envFrom:
    - secretRef:
        name: ext-service-secret
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
