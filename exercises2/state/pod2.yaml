apiVersion: v1
kind: Pod
metadata:
  labels:
    run: busy-with-pvc2
  name: busy-with-pvc2
spec:
  containers:
  - command:
    - sleep
    - "3600"
    image: busybox
    name: busy-with-pvc2
    resources: {}
    volumeMounts:
      - mountPath: "/etc/foo"
        name: myvol
  volumes:
    - name: myvol
      persistentVolumeClaim:
        claimName: mypvc
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
